# ACF JSON

When Advanced Custom Fields is used, commit field-group JSON here. Name and organize field groups by module so field definitions can travel with their templates and assets. Do not add database-dependent sample content.
